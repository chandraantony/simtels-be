exports.region = (data) => {
  const request = {
    name: data.name,
    description: data.description
  };
  return request;
};

exports.jobProgress = (data) => {
  const request = {
    name: data.name,
    description: data.description
  };
  return request;
};

exports.customer = (data) => {
  const request = {
    name: data.name,
    description: data.description
  };
  return request;
};

exports.services = (data) => {
  const request = {
    name: data.name,
    description: data.description
  };
  return request;
};

exports.role = (data) => {
  const request = {
    name: data.name,
    description: data.description
  };
  return request;
};
